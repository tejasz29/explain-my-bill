# Local dev: leave empty to use Vercel rewrites (relative /api/* paths)
# Production: set in Vercel dashboard (not needed if API_BASE_URL + rewrites configured)
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
